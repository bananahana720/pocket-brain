# Optional local flags for app + proxy dev
# Vite app (root .env.local)
# VITE_USE_AI_PROXY=true
# VITE_DEV_PROXY_WORKER=true
# VITE_WORKER_PROXY_TARGET=http://127.0.0.1:8787

# Worker local runtime values live in worker/.dev.vars.
# Keep insecure auth disabled outside local loopback testing.
# ALLOW_INSECURE_DEV_AUTH=false in production.

# For production deploy validation, ensure Clerk vars are set together when any is set:
# CLERK_JWKS_URL=
# CLERK_ISSUER=
# CLERK_AUDIENCE=
